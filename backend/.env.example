NODE_ENV=development
NETWORK=devnet

# Solana
RPC_ENDPOINT=https://api.devnet.solana.com
PROGRAM_ID=FqtrHgdYTph1DSP9jDYD7xrKPrjSjCTtnw6fyKMmboYk
# Base58-encoded 64-byte secret key of the server wallet that subsidizes gas fees.
# Generate with: solana-keygen new --no-bip39-passphrase --outfile fee-payer.json
# then: cat fee-payer.json | python3 -c "import sys,json,base58; print(base58.b58encode(bytes(json.load(sys.stdin))).decode())"
# NEVER commit the real value — use a secrets manager (e.g. Google Secret Manager) in production.
FEE_PAYER_PRIVATE_KEY=replace_me_with_base58_secret_key
# Intelligent scene response mode (natural but with a slight delay)
USE_QUICK_REPLY_ACK=true
# Ultra-fast ACK mode (default, <5ms response time)
# USE_QUICK_REPLY_ACK=false

# AI (Cartesia)
# High-Fidelity API Configuration
CARTESIA_API_KEY=sk_car_xxxxxxx
CARTESIA_MODEL_ID=sonic-3
CARTESIA_VOICE_ID=6ccbfb76-1fc6-48f7-b71d-91ac6298247b

# AI (Groq)
GROQ_API_KEY=replace_me
GROQ_MODEL=llama-3.1-8b-instant
GROQ_TIMEOUT_ACK_MS=200
GROQ_TIMEOUT_BG_MS=2000
# EDGE
USE_EDGE_PIPELINE=true
EDGE_LLM_ENDPOINT=http://http://127.0.0.1:11434/api/chat
EDGE_TTS_ENDPOINT=http://127.0.0.1:5002/api/tts
# Audio (Deepgram)
DEEPGRAM_API_KEY=replace_me
DEEPGRAM_TTS_MODEL=aura-2-orion-en
DEEPGRAM_STT_MODEL=nova-2

# IoT
MQTT_BROKER_URL=mqtt://test.mosquitto.org
MQTT_TOPIC=orin/room/control

# Persistence
REDIS_URL=redis://127.0.0.1:6379
STATE_PROVIDER=redis

# API
API_HOST=127.0.0.1
API_PORT=3001
ALLOWED_ORIGIN=http://localhost:3000
API_KEY=orin_secret_key_2026_dev

# firebase
FIREBASE_KEY=firebase-adminsdk.json

# ─────────────────────────────────────────────────
# Duffel Stays API (Hotel Search → Quote → Booking)
# ─────────────────────────────────────────────────
# Get your Test token from: https://app.duffel.com/access-tokens
# Test tokens start with "duffel_test_..." — safe for sandbox use.
# Live tokens start with "duffel_live_..." — charges real money!
# Leave empty to disable the /api/v1/stays/* endpoints.
DUFFEL_API_KEY=duffel_test_replace_me

PUSD_TOKEN_MINT_ADDRESS=8y7gWKDiGjkb6q9BLsctFghHTRzTMvbgjVx91BNnRrLK